import React from 'react'
import { observer } from 'mobx-react'

const BranchesList = observer(({ storage: { stagedFiles, onStagedFilesChanged } }) => {
  return <div />
})

export default BranchesList
